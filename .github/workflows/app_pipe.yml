name: 📦 CI/CD - Testes e Qualidade (TDD)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '40 17 * * 3'  # Toda quarta às 17:40 UTC

env:
  PYTHON_VERSION: '3.12'
  PIP_CACHE_DIR: ~/.cache/pip

jobs:
  lint-and-quality:
    name: 🔍 Lint e Qualidade de Código
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - uses: actions/cache@v4
        with:
          path: ${{ env.PIP_CACHE_DIR }}
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
          pip install ruff isort pylint

      - name: Verificar Lint (Ruff)
        run: ruff check .

      - name: Verificar Imports (Isort)
        run: isort src/ --profile black

      - name: Verificar Qualidade (Pylint)
        run: |
          pylint src/ tests/ || echo "⚠️ Pylint encontrou problemas, mas o job continuará."

  test-backend-unit:
    name: 🧪 Testes Backend Unitários
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Instalar dependências backend
        run: pip install -r requirements.txt -r requirements-dev.txt pytest pytest-cov codecov
      
      - name: Rodar testes backend unitários
        run: pytest --cov=src/backend --cov-report=xml:coverage-backend.xml tests/backend/unit/
      
      - name: Upload coverage backend unit
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage-backend.xml
          flags: backend,unit

  test-backend-integration:
    name: 🧪 Testes Backend Integração
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Instalar dependências backend
        run: pip install -r requirements.txt -r requirements-dev.txt pytest pytest-cov codecov
      
      - name: Rodar testes backend integração
        run: pytest --cov=src/backend --cov-report=xml:coverage-backend-integration.xml tests/backend/integration/
      
      - name: Upload coverage backend integração
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage-backend-integration.xml
          flags: backend,integration

  test-frontend-unit:
    name: 🧪 Testes Frontend Unitários
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
      
      - name: Instalar dependências frontend
        run: npm install
      
      - name: Rodar testes frontend unitários com coverage
        run: npm run test -- --coverage --coverageReporters=lcov --coverageReporters=cobertura
      
      - name: Upload coverage frontend unit
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/cobertura-frontend.xml
          flags: frontend,unit

  test-frontend-e2e:
    name: 🧪 Testes Frontend E2E
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
      
      - name: Instalar dependências frontend
        run: npm install
      
      - name: Rodar testes frontend e2e com coverage
        run: npm run test:e2e -- --coverage --coverageReporters=cobertura
      
      - name: Upload coverage frontend e2e
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/cobertura-frontend-e2e.xml
          flags: frontend,e2e

  test-full-integration:
    name: 🧪 Testes de Integração Full Stack
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Instalar dependências testes integração
        run: pip install -r requirements.txt -r requirements-dev.txt pytest pytest-cov codecov
      
      - name: Rodar testes de integração full com coverage
        run: pytest --cov=tests/integration --cov-report=xml:coverage-integration.xml tests/integration/
      
      - name: Upload coverage integração full
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage-integration.xml
          flags: integration

  security:
    name: 🔐 Análise de Segurança (Bandit)
    runs-on: ubuntu-latest
    needs: lint-and-quality
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - uses: actions/cache@v4
        with:
          path: ${{ env.PIP_CACHE_DIR }}
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Rodar Bandit
        run: |
          pip install bandit
          bandit -r src/ -ll -ii --skip B101 || echo "⚠️ Bandit encontrou problemas, mas o job continuará."

  create-issue-on-failure:
    name: 🚨 Criar Issue em caso de falha
    runs-on: ubuntu-latest
    needs: [lint-and-quality, test-backend-unit, test-backend-integration, test-frontend-unit, test-frontend-e2e, test-full-integration, security]
    if: failure()
    steps:
      - name: Criar Issue no GitHub em caso de falha
        env:
          GH_TOKEN_ISSUES: ${{ secrets.GITHUB_TOKEN }}
          REPO: DiasPedroQA/bookmark-converter
          WORKFLOW: ${{ github.workflow }}
          RUN_ID: ${{ github.run_id }}
          ACTOR: ${{ github.actor }}
          BRANCH: ${{ github.ref_name }}
        run: |
          ISSUE_TITLE="🔴 Falha na pipeline: $WORKFLOW"
          ISSUE_BODY="Pipeline falhou para o repositório $REPO\n\nWorkflow: $WORKFLOW\nRun ID: $RUN_ID\nBranch: $BRANCH\nExecutado por: $ACTOR\n\nVerifique os logs para detalhes."
          curl -X POST -H "Authorization: token $GH_TOKEN_ISSUES" -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/$REPO/issues \
          -d "{\"title\": \"$ISSUE_TITLE\", \"body\": \"$ISSUE_BODY\"}" || echo "⚠️ Falha ao criar issue"
